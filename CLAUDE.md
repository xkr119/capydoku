# Capydoku (카피도쿠)

> **새 세션이라면**: `../HANDOFF.md`(지금 어디까지 왔고 다음에 뭘 하는가)를
> 먼저 읽고 이 파일로 오면 된다. 이 파일은 **설계 원본이자 함정 목록**이다 —
> "왜 이렇게 되어 있는가"와 "무엇을 되돌리면 안 되는가"가 적혀 있다.
> 뭘 만들기 전에 관련 절을 읽으면 같은 실수를 두 번 하지 않는다.

링크드인 Queens 계열 로직 퍼즐(Meowdoku/Zoodoku와 같은 장르) + 카피바라 테마.
Flutter, **안드로이드 우선**, 오프라인 전용. 세 번째 부업 앱이다
(첫째 `../photo_tidy`, 둘째 `../nemologic` — **네모로직과 같은 설계 철학·구조를
공유하므로 뭘 만들기 전에 nemologic을 먼저 볼 것**).

규칙: N×N 보드, 행·열·색영역마다 카피바라 1마리, 서로 인접(대각 포함) 금지.
게임 규칙은 저작권 보호 대상이 아니라 클론이 합법이지만, **저들의 그림·이름·
스크린샷 문구를 베끼면 안 된다.** 우리 표현은 카피바라 픽셀아트다.

패키지명 `kr.tak.capydoku`. 스토어 등록 후 못 바꾼다.
스토어 제목 후보: `Capydoku : 카피바라 논리 퍼즐`. "치매 예방" 금지(정책 리스크),
"두뇌 게임·집중력" 계열 사용 — nemologic CLAUDE.md의 스토어 절 참고.

## 개발 환경

nemologic과 동일: Flutter `~/development/flutter`, **`dart analyze`만 사용**
(한글 경로에서 flutter analyze는 죽는다), 안드로이드 SDK `~/Library/Android/sdk`,
JDK `~/development/jdk`. 릴리스 APK는 반드시 실기기/에뮬레이터로 실행 확인
(R8 리플렉션 삭제 사고 — photo_tidy·nemologic 둘 다 겪었다. proguard 규칙을
nemologic에서 복사해 올 것).

**확인은 하되 싸게 할 것.** 이 앱은 눈으로 봐야 아는 것이 많아서 에뮬레이터
캡처가 늘어나기 쉽고, 그러다 한 세션에 토큰 864만을 쓴 적이 있다(2026-08-20).
캡처 한 장은 찍은 그때가 아니라 **남은 대화 내내** 값을 치른다. 규칙과 구체
절차(저장값 직접 세팅, `timeDilation`, 프레임 몽타주)는 `../HANDOFF.md`의
**"토큰을 아끼면서 확인하기"**와 **"연출 확인 요령"** 절에 있다.
**캡처 10장 또는 빌드 3회를 넘길 것 같으면 시작 전에 사용자에게 물을 것.**

## 엔진 (lib/engine/queens.dart) — 증명 끝난 것

1. **배치 생성** — 무작위 백트래킹 (행·열 유일 + 대각 인접 금지)
2. **영역 성장** — 퀸 자리를 씨앗으로 경계 비례 성장. **핵심: n/3개 영역을
   2칸에서 동결한다.** 균등 성장은 유일해가 거의 안 나와 n=8에서 퍼즐당
   15초가 걸렸다(시도 5만 번). 동결 후 5ms(시도 145번). 이 동결 로직을
   "단순화"하지 말 것.
3. **유일해** — 백트래킹 전수(n≤9라 즉시).
4. **LogicSolver** — 사람 규칙만: 행/열/영역 단독 후보 확정, 영역이 한 행/열에
   갇히면 밖을 지움, 한 수 읽기(놓으면 다른 행/열/영역 전멸 → 배제).
   음수 반환 = 논리로 안 풀림 = 폐기. **"찍기 없이 100% 논리" 보증이 여기서
   나온다. 백트래킹을 섞지 말 것.**
5. 난이도 = passes + 한수읽기×3. `_Rng`는 웹 정밀도 안전한 _mul32 사용,
   골든 테스트(7×7 시드 1)가 수열을 고정한다 — 깨지면 코드를 되돌릴 것.

성능 기준선: 7×7 생성 50개 ≈ 22ms, n=9도 개당 ~20ms.

## 캐릭터 애니메이션 — 관절 인형 (2026-08-20 오전, 이 앱의 심장)

**프레임 교체를 하지 않는다.** 그림 몇 장을 번갈아 보여 주는 방식으로는
"살아 있다"가 안 나온다는 것이 사용자 판정이었고, 그래서 3D 렌더 한 장을
부위로 분해해 매 프레임 다른 각도로 합성하는 방식으로 갈아엎었다.

- `tool/rig_parts.py` — `assets/mascot/capy_base.png`(651×900) 한 장에서
  `assets/rig/{body,head,jaw,lidl,lidr}.png`를 만든다. 머리/몸통 경계는
  가운데가 턱까지 내려오는 곡선(`head_cut_y`)이고, 양쪽 알파를 넓게 겹쳐
  페더링해 회전해도 이음매가 안 보인다. 눈꺼풀은 눈 **위쪽 이마 털을 떠 온
  것**이라 내려 덮으면 진짜로 감은 것처럼 보인다.
  - 파일 크기: 완전 투명한 픽셀의 RGB를 0으로 밀지 않으면 조각 하나가
    780KB가 된다(PNG가 투명 영역을 못 줄인다). 반투명 가장자리까지 곱하면
    렌더러가 알파를 한 번 더 곱해 테두리가 거뭇해지므로 **알파 0인 곳만** 지운다.
- `lib/art/capy_rig.dart` — `CapyPose`(고개 회전·끄덕·입벌림·깜빡임·숨·기울기·
  점프·좌우이동·눌림)를 캔버스에 직접 그린다. 위젯 트리가 아니라 CustomPainter인
  이유는 보드에 카피가 열 마리까지 동시에 움직이기 때문.
  `CapyPerformer`가 시간축을 굴리며 두리번·갸웃·깜빡임을 알아서 하고,
  `CapyController.play(CapyAct.eat/cheer/dance/startle)`로 밖에서 시킨다.
  **`_pivotX/_pivotY`는 rig_parts.py의 상수와 같아야 한다** — 원본 렌더를
  갈아끼우면 두 파일의 좌표를 함께 다시 재야 한다.
- **앞발도 따로 돈다**(`arml/armr`, 어깨가 축). 머리만 흔들면 목각 인형이다.
  도려낸 자리는 **안쪽 털을 좌우로 반사해서** 덮는다. 원본을 그대로 두면
  팔이 움직일 때 제자리의 팔이 유령처럼 비친다.
  **이 절의 아래 다섯 줄은 전부 실제로 밟은 함정이다.** 리그를 다시 뽑기 전에
  반드시 읽을 것 — 같은 자리에서 세 번 틀렸다.
  - **"팔이 네 개"였다**(2026-08-20 저녁에 잡았다). `SPECS`의 앞발 타원이
    실제 팔보다 작고 안쪽으로 치우쳐 있어서, 도려낸 것은 옆구리 한 조각이고
    **진짜 팔은 몸통에 그대로 남아 있었다.** 평소엔 팔이 제 그림자 위에
    겹쳐 있어 티가 안 나다가, 먹을 때 앞발이 맨 앞으로 올라가면 남은 팔이
    드러나 넷으로 보였다. 덩치가 클수록 심했다(어른은 몸 폭의 28%짜리
    "아기 팔"이 달려 있었다).
  - **앞발은 아래에서 안쪽으로 벌어진다.** 기둥 폭(`arms`의 타원)으로만
    자르면 **안쪽 발가락이 몸통에 남는다** — 실기기에서 "발톱이 몸에 여전히
    붙어 있다"는 지적을 받은 것이 이것이다. 그래서 `Spec.paws`에 **발가락의
    가장 안쪽 x**를 따로 적어 두고, 자르는 범위를 거기까지 넓힌다.
    발톱 좌표는 팔의 세로 구간 안에서 **몸통 중간밝기의 42% 미만** 픽셀
    덩어리로 찾는다(뒷발이 섞이므로 y 범위를 반드시 제한할 것).
  - **메우는 자국의 흐림은 12까지만.** 20으로 두었더니 가장자리의 절반쯤
    덮인 구간에 발톱이 걸쳐 그대로 비쳤다. 발톱은 아주 어두워서 30%만
    남아도 발톱으로 보인다.
  - **메우는 털은 안쪽 털을 좌우로 반사해서 채운다**(numpy `pad(mode='reflect')`).
    여기까지 두 번 틀렸다:
    ① 멀리서 통째로 **평행이동**해 덮었더니, 몸이 둥글어 안쪽은 밝고 옆구리는
    어두워 **덮은 자국이 다시 팔 모양으로** 보였다("움직이는 팔이랑 겹쳐
    보인다"). ② 띠만 있는 **투명 그림**으로 만들었더니, 마스크가 띠보다 넓은
    만큼 몸통이 지워지고 마지막 `putalpha`가 실루엣만 되살려 **몸통에 검은
    세로띠**가 생겼다. 패치는 반드시 `src.copy()`에서 시작할 것.
    반사는 이음매에서 색이 정확히 이어지고, **뜨는 폭은 반대쪽 팔 앞에서
    멈춘다** — 넘어가면 반대쪽 팔이 거울처럼 딸려 와 유령 팔이 하나 더 생긴다.
  - **메우는 자국은 타원이 아니라 알약(capsule) 모양**이다. 팔은 위아래로
    곧은 기둥이라 타원으로 덮으면 어깨와 발끝 언저리가 오목하게 남는다.
  - **좌표는 눈대중하지 말고 재라.** 팔과 몸통 사이에는 어두운 **세로 이음선**이
    있다. 그 띠에서 밝기의 열 평균을 내면 골이 정확히 그 선에 잡히고,
    바깥쪽 끝은 실루엣 가장자리다. 그 둘 사이가 팔이다. 세로 범위는 겨드랑이
    ~발톱 아래(발톱은 몸통 중간밝기의 45% 미만인 픽셀 덩어리로 찾는다.
    뒷발이 같이 잡히므로 y 상한을 둘 것). 실측 스크립트는 대화에만 있었고,
    결과는 `SPECS`에 들어가 있다.
  - 타원은 **실루엣 바깥 12px부터 이음선 안쪽 8px까지** 넉넉히 잡는다.
    바깥으로 넘치는 것은 마지막에 원본 알파로 다시 잘리므로 공짜고,
    이음선을 덮어야 몸통에 팔 자국이 안 남는다.
- 혼자 하는 큰 동작: **하품**(입을 1.7배까지 벌리고 고개를 젖힌다 — 그래서
  `jawOpen`은 1을 넘길 수 있고 입 안쪽 어두운 부분도 실제 입보다 길다)과
  **배 긁기**(한 앞발이 배를 문지른다). 셋에 하나꼴로 이게 나온다.
- **먹는 동작은 5초**(`eat`), 특별 먹이는 5.4초(`feast`). 먹이는 건 이 게임에서
  가장 큰 보람이라 짧으면 준 보람이 없다. 흐름은 덥석 → **와그작 와그작
  와그작**(몰아 씹고 잠깐 쉬기를 세 번 — 일정한 속도로 씹으면 기계처럼 보인다)
  → 꿀꺽 → 흐뭇. `feast`는 같은 흐름을 1.45배 크게 타고 한 입마다 통통 뛴다.
  홈의 먹이 그림도 이 리듬에 맞춰 **한 입씩 뭉텅** 사라지고(고르게 줄면 녹아
  없어지는 것처럼 보인다) 한 입마다 소리와 진동이 난다.
- 조각은 `CapyRigImages.load()`로 앱당 한 번만 디코딩한다(스플래시에서 예열).
- **캐릭터가 일곱이다.** 성장 5단계(`stage1`~`stage5`) + 배우자(`mate`) +
  퍼즐 칸 얼굴(`face`). `CapySkins.load(이름, 픽셀)`로 필요할 때 불러오고,
  **그릴 크기에 맞춰 디코딩한다** — 일곱 벌을 원본 해상도로 물면 60MB가 넘는다.
  좌표는 `tool/rig_stages.py`가 출력한 값을 `CapySkins._coords`에 옮겨 적었다.
- (옛 메모) 한때는 스킨이 둘이었다 — 전신과 얼굴. 퍼즐 칸은 작아서
  전신을 넣으면 표정이 몇 픽셀이 되고 그러면 캐릭터가 아니라 무늬로 보인다.
  스킨마다 회전축·눈 위치가 다르고, 그 숫자는 `rig_parts.py`가 실행 끝에
  출력한다(원본을 바꾸면 옮겨 적을 것).
  - 퍼즐 칸 얼굴(`face`)은 **청소년 렌더**에서 뽑는다(`FACE_FROM`). 원본
    카피는 둥글고 납작해 "빵떡" 소리를 들었다. 주둥이가 제대로 나온 얼굴이
    칸 안에서 캐릭터로 읽힌다.
  - 한때 얼굴에 흰 스티커 테두리를 둘렀다가 **뺐다** — 칸 색과 털색이 비슷한
    문제보다 테두리의 어색함이 더 컸다. 다시 넣지 말 것. 대비가 정 아쉬우면
    테두리 말고 팔레트(`Palette.regions`의 갈색 계열)를 손보는 쪽이 맞다.
- **흐뭇한 표정**(`CapyPose.smile`) = 눈을 다 감기고 **입을 살짝 벌린다**.
  한때 감은 눈 위에 ∧ 곡선을 그렸는데 3D 음영 위의 납작한 선이라 붙여 놓은
  티가 났다(사용자 판정: "억지 같다"). **선을 그리지 말 것.**
  눈꺼풀은 눈보다 조금 큰 정도로만 떠 온다 — 크게 뜨면 이마 털의 음영이
  주변과 어긋나 눈 둘레에 고리가 생긴다(46×42로 키웠다가 되돌렸다).
  그래서 눈꺼풀은 **완전히 감는 데만** 쓰고 반쯤 감은 상태는 안 쓴다.

### 퍼즐 칸의 카피는 다같이 똑같이 움직인다

`CapyPerformer(synced: true)`. 제각각 움직이면 시선이 흩어져 아무것도 안 보인다
(레퍼런스 고양이 게임도 맞춰 움직인다). 구현은 난수도 예약도 쓰지 않고
**전역 프레임 시각만으로** 안무를 계산한다 — 티커의 경과 시간은 위젯이 생긴
시점마다 달라서 쓸 수 없다. 3.9초 한 마디에 동작 하나(두리번 / 깊은 끄덕 /
갸웃하며 눈웃음)를 하고 남는 동안 다같이 한 번 깜빡인다. **동작을 늘리지 말 것**
— 종류가 많아지면 그게 곧 중구난방이다.

## 게임 (있는 것 — 2026-08-20 에뮬레이터 릴리스 검증)

- **아트**: `lib/art/capy_art.dart` — 밈 스타일 카피바라 SVG(디자인 캔버스에서 확정).
  토큰(흰 스티커 테두리)·귤캐피(홈)·온천캐피(완성)·놀람캐피(실수).
  **캐릭터 교체 = 이 파일 + tool/gen_icon.py 재생성이 전부다.**
  단, 홈·타일·완성 화면의 살아 있는 카피는 전부 위의 리그가 그린다.
- **소품**은 `lib/art/props.dart`에 벡터로 그린다(당근·수박·당근 바구니).
  - **먹이는 리그 *안*에서 그린다**(`HeldFood`, 아래턱 바로 밑 층). 밖에 따로
    띄우면 앞발·주둥이에 안 가려져 "화면에 붙은 스티커"로 보인다. 날아가는
    동안만 따로 그리고, 입에 닿는 순간 리그로 넘긴다.
    - **먹은 만큼 끌어올린다**(`CarrotPainter.gone`/`WatermelonPainter.gone`).
      제자리에 두면 베어 문 자리가 입에서 멀어져 허공에서 씹히고, 끝에는 턱
      아래 반쯤 남은 당근만 떠 있다. 두 페인터의 `gone`이 그 길이를 준다 —
      먹는 방향을 바꾸면 두 곳을 함께 고칠 것.
    - 당근은 **뿌리가 위로** 입에 들어가고 잎이 아래로 내려온다(세로 뒤집기).
      잎을 위로 두면 잎사귀를 먹는 그림이 된다. 그리고 **비스듬히 문다**
      (`HeldFood.carrotTilt`) — 똑바로 뒤집어 물면 통째로 삼키는 그림이다.
      기울이는 축은 **문 자리**여야 당근만 기울고 입은 제자리에 있다.
    - **먹는 동작은 먹이가 출발할 때 건다**(`main.dart`의 `_throwFood`).
      도착한 뒤에 걸면 앞발이 반 박자 늦게 올라와 "벌써 먹고 있는데 손이
      뒤따라오는" 그림이 된다. 동작의 첫 구간(덥석, `ap < 0.10` ≈ 500ms)이
      나는 시간(`_Morsel.flightEnd` ≈ 495ms)과 같게 맞춰 뒀다 — 그래서
      **앞발과 벌린 입이 먹이를 맞이한다.** 한 입씩 사라지는 리듬(`0.70`)도
      리그가 꿀꺽하는 시각에 맞췄다. 셋 중 하나라도 어긋나면 티가 난다.
    - **먹이 크기는 앞발이 못 닿는 것을 메우는 값이다**(`kFoodSize` 0.42 /
      `kFeastSize` 0.48). 앞발은 어깨를 축으로 도는 조각이라 입까지 끝내
      못 올라온다. 먹이가 크면 손이 먹이의 아래쪽을 받치는 것처럼 읽힌다.
      **`main.dart`의 날아오는 먹이도 같은 상수로 자란다** — 다르면 입에
      닿는 순간 크기가 튄다.
    - **자른 면이 정확히 입에 와야 한다.** 페인터 안에서 자른 면은 위에서
      `WatermelonPainter.centerY`만큼 내려간 곳이므로 그릴 때 그만큼 끌어올린다.
      눈대중으로 0.18을 썼다가 자른 면이 입보다 반 뼘 아래에 놓여 주둥이가
      허공을 씹었다. 당근(뿌리 끝이 입에 온다)과 **같은 규칙**이다.
      날아오는 수박(`main.dart`의 `top`)도 같이 고쳐야 넘겨받는 순간 안 튄다.
    - 수박은 **자른 면(위)부터** 베어 먹고 껍질이 남는다. 빨간 반지름을 줄이면
      속살이 안쪽으로 사라져 껍질이 자라나는 것처럼 보인다 — 위에서부터 잘라야 한다.
      크기는 당근보다 크다(0.38 대 0.34) — 특별 먹이는 한눈에 다른 등급으로
      읽혀야 한다. 물고 있을 땐 `grounded: false`로 **발밑 그림자를 끈다**
      (입 앞에 그림자가 떠 있으면 붙여 놓은 그림처럼 보인다).
    - **날아오는 먹이는 물릴 각도·크기로 정확히 착지한다.** 한 바퀴를 돌되
      끝나는 각이 리그가 그리는 각과 같아야 한다(수박 0, 당근 π+기울기).
      크기도 바구니 크기에서 물릴 크기로 자란다 — 어중간하게 두면 입에 닿는
      순간 각이 홱 돌거나 두 배로 커진다.
    - 크기는 캔버스가 아니라 **캐릭터의 실제 키**(`CapySkin.fill`)에 비례한다.
  - **앞발은 먹이를 물고 있을 때만 맨 앞 층에 그린다**(`_RigPainter.arms`).
    평소엔 몸통 바로 위지만, 그 자리에 두면 앞발이 먹이에 가려서 그냥 입에
    달라붙은 먹이로 보인다. 단 **머리 묶음 밖**에 그려야 한다 — 안에 넣으면
    고개를 돌릴 때 앞발이 따라 돌아 어깨가 빠진 것처럼 보인다.
  - **진짜로 쥐고 먹게 할 수는 없다.** 어깨를 축으로 돌려도 앞발이 입까지는
    못 올라온다. 다만 팔을 통째로 도려내고부터는 몸통에 남는 자국이 없어서
    예전 한계(0.34rad)보다 더 올릴 수 있다 — 지금은 0.42rad에 -0.050이고,
    팔이 길어진 만큼 같은 각도로도 훨씬 크게 움직인다.
    앞발로 감싸 쥐는 그림이 필요하면 **앞발을 든 새 원본 렌더**가 있어야 한다.
    아기(`stage1`)는 아예 앞발을 안 나눴다 — 몸통에 붙어 있어 안 움직인다.
  - **특별 먹이는 수박이다.** 카피바라의 상징이 수박이라 한눈에 다른 등급의
    간식으로 읽힌다. 처음엔 황금귤이었는데 당시 목숨 표시(귤)와 겹쳐 혼란스러웠고,
    황금당근으로 바꿨더니 "색만 다른 당근"으로 보였다. 되돌리지 말 것.
  - 바구니 당근은 **뾰족한 끝이 바구니 속 깊이 박힌 채로** 그린다(회전축도
    그 끝). 테두리 위에 얹으면 담긴 게 아니라 올려 둔 것으로 보인다.
    바구니 앞면의 당근 문양은 비었을 때도 무슨 바구니인지 알려 준다.
  당근은 날아가고 씹혀 사라지므로 매 프레임 다른 모습이어야 해서 이미지로
  둘 수 없다. `lib/art/effects.dart`에 뾰잉(`PoingCurve`/`PoingIn`/`PoingBurst`)과
  떠다니는 꽃가루, `lib/art/scenery.dart`에 풀밭·발밑 그림자가 있다.
- **조작**: 탭1 = X, 탭2 = 카피 배치 시도. 정답이면 고정 + 주변 자동 X
  (행·열·색영역·인접 — 이 장르 표준 QoL). 틀리면 하트 -1, 놀람캐피 +
  위반한 규칙 칩이 빨갛게 강조(색깔/행열/인접 구분). X 지우기 = 길게 누르기.
- **하트 3개** 소진 → 실패 오버레이(다시 풀기).
- **힌트는 하루 풀이다**(`Progress.hintsToday`, 카피 3 / X 3, 자정 리셋).
  예전엔 게임 화면을 열 때마다 3개씩 채웠는데, 그러면 힌트가 떨어져도
  **뒤로 나갔다 다시 들어오면 공짜로 차서** 리워드 광고를 볼 이유가 하나도
  없었다(그 광고가 이 앱의 핵심 수익원이라고 적어 두고서). 힌트는 희소해야
  광고에 값이 붙는다. 레퍼런스도 하루 단위다. **되돌리지 말 것.**
  - 다 썼을 때 **하루 단위라고 알려준다** — 판마다 차던 것이 안 차면
    버그로 보이지, 자정 리셋이라고는 생각하지 못한다.
  - 완성 보너스는 남은 개수가 아니라 **이 판에서 안 쓴 만큼**으로 센다
    (`_hintsUsed`). 남은 개수로 세면 아침에 쓴 힌트가 그날 남은 판 전부의
    점수를 깎는 이중 처벌이 된다.
- **판에서 당근을 줍는다** (2026-08-20 저녁). 예전엔 깨야만 당근이 생겨서
  판을 푸는 동안은 돌봄과 아무 관계가 없었다. 그리고 당근이 빠듯했다 —
  포만감은 시간당 -4로 빠지는데(당근 하나 +22) 판당 1~2개라 하루 세 판을
  꼬박 해야 겨우 맞았다.
  - 규칙은 **연속 정답 콤보**다(`_combo`). 실수 없이 세 번 맞힐 때마다 당근
    하나. 실수하면 0으로 끊긴다. 힌트로 놓은 것도 콤보를 올린다 — 힌트는
    판당 3회로 묶여 있어 캘 수 없고, 무엇보다 규칙이 하나여야 한다:
    **실수만 안 하면 다 받는다.**
  - 그래서 상단에 **"주운 것 / 얻을 수 있는 것"**(`n ~/ 3`)을 미리 보여줄 수
    있다. 한 번도 안 틀렸을 때의 값이라 거짓말이 아니고, "실수하면 놓친다"가
    눈에 보인다. 순수 랜덤이었다면 총량을 못 보여준다.
  - **지갑에는 깰 때 한꺼번에 들어간다.** 주울 때마다 넣으면 세 칸만 맞히고
    나갔다 들어오기를 반복해 무한히 캘 수 있다. 완성 화면은 합만 보여주지
    말고 **"주운 당근 2 + 클리어 2 = 4"**로 쪼개 준다 — 합만 보면 판에서
    주운 보람이 사라진다.
  - 콤보와 주운 개수는 **칸과 함께 저장한다**(`Progress.saveBoardMeta`,
    `bdm.<슬롯>`). 칸만 저장하면 앱을 껐다 켰을 때 콤보가 0부터 다시 세어져
    같은 판에서 당근이 한 번 더 나온다. "다시 풀기"는 이 값을 지운다.
  - 연출은 점수 비행(`_ScoreFly`)을 그대로 쓴다 — `carrot: true`면 숫자 대신
    당근을 그리고 **점수 카운터가 아니라 당근 카운터로** 날아간다.
    같은 곳으로 보내면 무엇이 늘어난 건지 안 보인다.
  - 상단 표시가 셋(카피·당근·하트)이라 좁은 폰에서 한 줄에 안 들어간다.
    `FittedBox(scaleDown)`으로 **줄어들게** 두었다(줄바꿈은 안 된다 — 이 줄이
    높아지면 그만큼 판이 작아진다). pill 좌우 여백도 18 → 12로 줄였다.
- **레벨 커브**(`lib/game/levels.dart`): 1:4×4, 2~3:5, 4~5:6, 6~7:7, 8:8,
  9:9, **10~: 10×10 무한**. 앞의 아홉 판은 거의 매번 판이 커진다 — 초반의
  유일한 보상이 그것이다. 10판째가 곧 본 게임의 시작이고 **배너 광고도
  거기서 시작한다**(전면은 레벨 20부터).
  - 예전엔 18판째에야 10×10에 닿았다. 8×8·9×9를 네 판씩 반복하는 동안은
    "커졌다"가 없어서 그 구간이 밋밋했다. 그렇다고 7×7에서 곧장 10×10으로
    뛰면 칸 수가 두 배가 되어 벽이 되므로, 한 판씩이라도 거쳐 간다.
  - **한 수 읽기는 목표 난이도 10부터**(대략 레벨 60) 허용한다. 그전에는
    소거만으로 풀린다. 예전엔 `level < 10`으로 막았는데 10×10이 시작되는
    레벨이 마침 10이라 **판이 커지는 바로 그 순간 방어선이 풀렸다.**
  - 소거만 쓰는 판은 난이도가 **6 언저리에서 천장**을 친다. 그 위를 목표로
    잡으면 맞는 판이 없어 후보를 끝까지 다 만들어 본다.
  - 어떤 시드를 쓸지는 **미리 구워 둔다**(`tool/bake_levels.dart` →
    `lib/game/level_seeds.dart`, 레벨 400까지). 굽기 전에는 운 나쁜 레벨이
    4초 가까이 걸렸다. 규칙을 고치면 다시 구워야 하고, **출시 뒤에는 다시
    굽지 말 것** — 전 사용자의 판이 통째로 바뀐다.
- **규칙 셋은 칸을 나눠 그린다.** 한 장의 흰 카드에 몰아넣으면 셋이 한
  덩어리로 보여 눈이 어디서 끊어야 할지 모른다. 각각 테두리와 배경을 주고,
  어긴 규칙만 빨갛게 세운다(레퍼런스도 같은 구성).
- **남는 세로는 상단 바 바로 아래에서 흡수한다.** 표시·설명·판·조작부는
  한 덩어리로 **아래에 몰아 둔다**(`Expanded` 안에서 `MainAxisAlignment.end`).
  두 번 틀렸다: 가운데 두면 설명과 판 사이가 벌어져 둘이 따로 놀고, 그렇다고
  판을 위로 붙이면 **손을 화면 위까지 올려야 해서 한 손으로 못 쓴다.**
  이 게임은 판을 수십 번 두드리는 게임이라 **엄지가 닿는 높이가 먼저다.**
- **이름은 뱃지에 담지 않는다**(홈). 카드처럼 두르면 UI 부품이 되는데 이건
  이 카피의 이름이다. 성장 게이지 위 왼쪽에 **스티커 레터링**(흰 글씨 +
  진한 갈색 테두리, `_StickerName`)으로 놓고 살짝 기울인다. 갈색 글씨에 흰
  후광만 깔았을 때는 **풀색과 명도가 비슷해 그대로 묻혔다** — 후광으로는 안
  떨어진다. 속을 희게 비우고 테두리를 둘러야 배경이 무엇이든 읽히고,
  만화 로고처럼 보여서 이름이 UI 부품이 아니라 이름으로 읽힌다. 연필 버튼도 없앴다 — 이름은 한 번 짓고
  거의 안 바꾸므로 **설정 안**이 제자리다. 이름을 누르면 카피가 대답하는데
  **반기지 않는다**(`CapySays.calledByName`) — 부르면 듣기는 하는데 딱
  그만큼만 하는 것이 이 캐릭터다.
- **가로 여백은 판 밖의 것들에만 준다**(`_boardSide` 6 + `_inset` 12).
  화면 전체에 여백을 두면 그만큼 판이 작아진다. 세로가 아니라 **가로가**
  판 크기를 정하는 화면이라(판은 정사각형) 좌우 20씩을 빼면 칸이 그대로
  작아진다. 상단 바·칩·조작부만 안쪽으로 밀고 판은 화면 끝까지 쓴다.
- 폰트 Jua(OFL, assets/fonts에 번들 — google_fonts 패키지는 런타임 네트워크라 금지).
- 세로 고정, 라이트 전용(크림 테마가 곧 브랜드).
- **런처 아이콘**(`tool/gen_icon.py`) = 퍼즐 영역 배경 + 카피 얼굴 크게.
  이 장르 아이콘의 문법이다(레퍼런스도 같은 구성). 전신을 넣으면 48dp에서
  무엇인지 안 보인다. 얼굴은 리그와 같은 조각(`assets/rig/h_head.png`)이라
  앱 안의 카피와 정확히 같다. 적응형 배경도 이미지(`icon_bg.png`)를 쓴다 —
  단색이면 퍼즐 게임으로 안 읽힌다. 리그를 다시 뽑으면 아이콘도 다시 뽑을 것.
  - **배경은 팔레트의 차가운 색만 쓴다**(보라·청록·연두). 카피 털이 주황~황갈
    색이라 주황·갈색 칸을 깔면 서로 묻혀 형체가 안 보인다. 앱 안은 크림톤이
    브랜드지만 아이콘은 대비가 먼저다.
  - **카피는 아래 테두리에서 올라온다**(`FACE_TOP` 0.32 / `FACE_H` 0.62,
    둘 다 완성된 아이콘 기준). 가운데에 얌전히 얹으면 증명사진이고, 사방에
    배경이 남으면 얼굴이 작아 보인다. 턱은 아래로 넘겨 자른다.
    더 키운 안(0.28/0.70)은 배경이 거의 사라져 **"퍼즐 게임"이라는 신호까지
    같이 사라졌다** — 얼굴과 배경 중 하나를 고르는 것이 아니라 **위쪽에 판이
    한 줄은 남아야** 한다.
  - **`assets/icon/icon.png`만 보고 판단하지 말 것.** 요즘 폰은 적응형
    아이콘을 쓰고 그건 **바깥 1/3이 잘려 나간다**(108dp 중 가운데 72dp).
    레거시 파일만 열어 보다가 턱이 잘린 아이콘을 세 번 내보냈다.
    **반드시 `python3 tool/preview_icon.py`**로 런처가 자르는 대로 본다.
  - **`flutter_launcher_icons`가 전경을 `inset 16%`로 감싼다 — 매번.**
    XML에서 지워도 도구를 돌리면 되살아난다. 전경 이미지는 108dp 캔버스의
    가운데 68%로 줄어든 뒤 다시 바깥 1/3이 잘리므로, 이미지에 0.70으로
    그려도 화면에서는 **0.476**이다. "얼굴을 더 크게" 해도 폰에서는 그대로였던
    이유가 이것이고, `preview_icon.py`가 이 inset을 빼고 그리는 바람에
    미리보기가 계속 거짓말을 했다. 지금은 미리보기가 **XML을 읽어** 반영하고,
    `gen_icon.py`는 좌표를 **완성된 아이콘 기준**(`FACE_TOP`/`FACE_H`)으로
    적고 `_to_image()`로 되돌려 그린다. 둘 다 되돌리지 말 것.
  - 흰 테두리는 얼굴이 작을 때 필요했던 것이다. 얼굴이 8할을 덮고 배경이
    진보라인 지금은 대비가 충분해서 **0.010**으로 충분하다(0.038 → 0.016 →
    0.010). 굵을수록 안전 영역에 들어가는 얼굴이 작아지기도 한다.
  - 아이콘을 다시 뽑으면 **스토어 그래픽도 함께**:
    `python3 tool/gen_icon.py && dart run flutter_launcher_icons`
    `&& python3 tool/gen_store_graphics.py`

## Meowdoku 관찰 노트 (사용자 실플레이에서 확보)

- 초반 레벨은 첫 고양이가 미리 놓여 있고(스타터), 레벨 11쯤부터 없음 → 구현함.
- 힌트 2종: 위치 공개(고양이)·X 공개(전구). 소진 시 ▶ 뱃지 → 리워드 광고 충전.
- 목숨 소진 화면: 우는 캐릭터 + "멸치 3개 받기(AD)" 리워드 → 우리는 "하트 3개
  받기"를 실패 다이얼로그에 넣었다(리워드 광고로 실제 충전).
- 데일리 챌린지는 레벨 21에서 해금(초반 몰입 우선). "N%가 이 레벨을 완료" 토스트.
- 시즌·랭킹·프로필(아바타/프레임): **v1에서 하지 않는다.** 진짜 글로벌 랭킹은
  백엔드가 필요하지만, 서버 없이 하려면 **Google Play Games Services 리더보드**
  (구글 호스팅, 무료, games_services 플러그인)가 정답. 시즌·선물 배포까지 가면
  운영 업무가 생겨 "유지보수 없는 앱" 전제가 깨진다. 프로필 아바타 수집은
  로컬만으로 가능해서 후순위 후보.

## 게임을 감싸는 것들 (2026-08-20 심야 패스)

- **카피의 목소리** (`lib/game/capy_says.dart`): 클리어 타이틀·멘트·실패 멘트 +
  **이름을 불렀을 때의 대답**(`calledByName`) 풀.
  게으르고 센치하고 나태한 말투가 캐릭터 정체성 — 밝고 호들갑스러운 문구 금지.
  레벨·점수 해시로 결정적으로 골라 같은 판엔 같은 말.
- **카피 리그(NPC 랭킹)는 걷어냈다** (2026-08-20 저녁). 서버 없이 24시간
  랭킹을 돌리고 상대를 "누가 봐도 NPC인 카피바라"로 두어 정직성은 지켰는데,
  **정직할수록 더 의미가 없었다** — 봇인 걸 아는 상대와의 등수라 이기면
  공허하고 지면 가짜한테 진 것이다. 무엇보다 "게으르고 센치한 카피바라를
  돌본다"는 이 앱의 정서와 정반대였고, 레퍼런스는 실제로 리뷰에서 그 기능으로
  욕을 먹고 있었다. 재방문 훅은 **오늘의 퍼즐+스트릭**과 **카피 돌보기**
  (포만감이 시간당 빠진다) 둘이면 충분하고, 둘 다 정서에 맞는다.
  되살리지 말 것. 하루 점수(`dailyScore`)는 주간 차트가 계속 쓴다.
- **일일 출석** (`lib/ui/checkin_sheet.dart`): 랭킹이 있던 홈 상단 자리를
  물려받았다. 7일 도장판, 보상은 **당근**(2·2·3·3·4·4·5)이고 7일째에 수박이
  하나 더 붙는다. 새 개념을 하나도 안 만드는 것이 요점이다 — 보상이 곧
  당근이라 "매일 와야 카피가 안 굶는다"로 이야기가 이어진다.
  오늘의 퍼즐 스트릭과 겹치지 않는다: **출석은 켜기만 하면 되고(문턱 0)**,
  오늘의 퍼즐은 8×8 최고난도를 풀어야 한다.
  가족 사건 **다음에** 띄운다 — 성장·결혼이 훨씬 큰 사건이라 출석 창이
  그 앞을 막으면 안 된다.
- **광고** (`lib/core/ads.dart`, 전부 테스트 ID — 출시 전 교체, 매니페스트와 함께):
  배너 = 게임 화면 **맨 아래에 여백 없이** 붙인다(콘텐츠 사이에 끼우면 판이
  좁아지고 광고만 더 눈에 띈다).
  - **그래서 게임 화면은 몰입 모드다**(`SystemUiMode.immersiveSticky`,
    나갈 때 `edgeToEdge`로 되돌린다). 3버튼 내비게이션을 쓰는 기기에서는
    시스템 바가 배너 위를 덮었다 — 보기 싫은 정도가 아니라 **광고를 가리는
    정책 위반**이다. 가장자리를 쓸어올리면 바가 잠깐 올라왔다 다시 숨는다.
  - **`SafeArea`를 쓰면 안 된다.** 숨긴 상태바를 0으로 보고해서 뒤로가기
    버튼이 카메라 구멍 밑으로 들어간다. `MediaQuery.viewPaddingOf`로 위쪽만
    직접 띄우되 **최소값을 둔다**(0으로 오는 기기가 있다).
  - **`windowLayoutInDisplayCutoutMode=shortEdges`가 함께 있어야 한다**
    (`values/styles.xml`, `values-night/`도). 없으면 전체 화면일 때 시스템이
    카메라 구멍이 있는 위쪽 띠를 **검게 레터박스로 막아** 크림색 배경이 잘린다. **레벨 4부터**(온보딩 보호). 전면 = 클리어 7판마다, 판 사이만.
  리워드 = 힌트 +3(카피/X 각각), 하트 3개 회복(실패 다이얼로그) — 핵심 수익원.
  **힌트가 하루 풀이라야 이 광고에 값이 붙는다**(위 "힌트는 하루 풀이다" 참고).
  proguard는 nemologic에서 복사(R8-WorkManager 사고 예방), 광고 포함 릴리스로
  에뮬레이터 크래시 0 확인.
- **연출**: 판 시작 시 타일이 좌상단부터 대각 파도로 다다다닥 등장(_intro),
  완성 화면 반짝이 별 + 온천 김(SteamOverlay, 절차적) + 색종이 + 햇살.
- **팔레트**: 레퍼런스 톤의 진한 색감으로 교체 (파스텔 아님 주의).
- 파일 구조: game_screen(보드·입력·흐름) / win_celebration / capy_says /
  league·league_screen / capy_token은 game_screen 내 / art(capy_art SVG,
  capy_motion 모션·김·프레임루프) / core(ads, palette, progress).

## 성장 5단계와 가족 (2026-08-20 낮)

**단계마다 캐릭터를 새로 그렸다.** 원본 하나를 비율만 바꿔 쓰는 방식은
"아기나 어른이나 달라지는 게 없다"는 판정을 받고 폐기했다. 다음 단계가
궁금해지려면 진짜로 다른 그림이어야 한다.

- 렌더는 Canva로 **한 장씩** 생성했다(격자 생성은 캐릭터가 붕괴된다).
  **원본은 `art_src/`에 두고 커밋한다.** 한때 `build/stage_raw/`에 있었는데,
  거기는 git이 무시하는 데다 `flutter clean`이 통째로 지우는 자리다 —
  캔바 할당량은 언젠가 소진되고 그때 이 그림들은 **다시 못 만든다.**
  `art_src/stage_raw/` → `tool/gen_stages.py`(배경 따기·발 높이·상대 크기 정렬)
  → `assets/stages/` → `tool/rig_stages.py`(부위 분해) → `assets/rig/stage*`.
- **크기 차이는 그림이 담고 있다.** 같은 캔버스 안에서 아기는 절반, 어른은
  가득 찬다. 그래서 화면에서는 **캔버스 높이를 고정**해야 한다 — 여기서 또
  단계 배율을 곱하면 아기가 점만 해진다(실제로 겪었다).
  말풍선·이름표는 캔버스가 아니라 `Pet.stageOf().scale`로 계산한 **실제 머리
  높이** 위에 놓는다.
- 해금: **30판마다 한 단계.** 아기 1 / 어린이 30 / 청소년 60 / 성인 90 / 어른 120.
  - 50판 간격이었을 때는 하루 20분씩 해도 어른까지 33일, 결혼 45일이 걸렸다 —
    캐주얼 퍼즐 이용자 대부분이 2주 안에 떠나므로 **사실상 아무도 결혼을 못
    봤다.** 다섯 단계와 가족은 이 앱이 파는 것 자체라, 못 보고 떠나는 건
    만들지 않은 것과 같다(`test/audit_manual.dart`로 잰 값이다).
- **가족의 먹이**: 당근은 **막내부터 차례가 돌아간다**(`Family.feedOrder`).
  막내만 계속 먹으면 나머지 식구의 먹는 모습을 영영 못 본다. 수박은 **온
  가족이 동시에** 먹는다 — 7판에 하나 나오는 귀한 것이니 다같이 먹는 그림이어야
  특별함이 눈에 보인다. 여럿이 먹을 때 저작음은 첫 조각만 낸다(겹치면 소음).
  포만감·기분은 값이 하나 그대로다 — 식구마다 스탯을 두면 게이지가 다섯 쌍이
  되어 돌봄이 숙제가 된다. 가족 단위로 읽는 편이 이 게임의 무게에 맞다.
- **가족 배치**(`Family.lineup`): 혼자면 가운데, 결혼하면 주인공이 왼쪽으로
  비키고 짝이 옆에 바짝 선다(살짝 겹침). 아이는 앞줄에 서고 발이 조금 더
  아래라 앞뒤가 생긴다. **만진 식구가 반응한다** — 손잡이를 식구마다 따로
  둔다(`_ctrlFor`). 예전엔 어디를 눌러도 주인공만 움직여 가족이 인형 같았다.
  이름표는 왼쪽 위에 있고 가족이 생기면 "○○ 가족"이 된다. 몸무게 배지는
  혼자일 때만 — 식구가 늘면 그 숫자는 의미가 없다.
- **어른 다음은 가족**(`lib/pet/family.dart`). 결혼 150 → 첫아이 190 →
  이후 40판마다 아이가 자라고 새 아기가 태어나고, 성인이 된 아이는 독립한다.
  40판마다 반드시 무언가 바뀌므로 레벨이 아무리 올라도 볼 것이 남는다.
  **새 그림은 배우자 한 장뿐이다** — 아이들은 아기~청소년 렌더를 재활용한다.
- 리깅 함정 둘(둘 다 성인·어른에서만 터졌다):
  팔 자리를 배 털로 메울 때 **부호를 뒤집으면** 몸 바깥의 투명 영역을 끌어와
  구멍이 뚫린다. 몸통 위쪽을 목 굵기로 좁히면 **넓은 몸은 옆구리가 잘려** 흰
  띠가 생긴다(그래서 단계 리그에서는 좁히지 않는다).
## 가족이 바뀌는 순간 — 연출 (2026-08-20 저녁)

성장도 결혼도 출산도 독립도 **레벨이 넘어가면 그냥 벌어져 있었다.** 어제
혼자였는데 오늘 옆에 짝이 서 있고, 아이가 소리 없이 하나 늘어 있고, 첫째는
어느새 사라져 있었다. 이 게임 후반의 사건이라고는 그것뿐인데 그걸 못 보고
지나가면 레벨을 올릴 이유도 같이 사라진다.

- 판정은 `lib/pet/family_event.dart`, 장면은 `family_event_scene.dart`.
  홈으로 돌아올 때(`didPopNext`)와 앱을 켤 때 못 본 사건을 찾아 차례로 재생한다.
- **컷신을 따로 만들지 않는다.** 배경·발이 닿는 선·식구가 서는 자리를 홈과
  똑같이 계산한다. 짝꿍이 걸어 들어와 서는 자리가 내일부터 짝꿍이 살 자리여야
  "우리 집에 벌어진 일"로 읽힌다. 넘어갈 때도 밀지 말고 **페이드**할 것.
- 움직임은 `CapyPerformer`(자동 안무)가 아니라 **`CapyRig`에 자세를 직접
  먹여서** 만든다. 걷기·마주보기·뒤돌아보기는 안무에 없고, 자리를 옮기는 것은
  애초에 리그 밖의 일이라 위치도 같이 움직여야 한다. 걸음은 **통통 튀는 리듬과
  좌우 기울기**로 만든다(발이 없으니 미끄러뜨리면 컨베이어에 실린 것처럼 보인다).
- **크기를 바꿀 때는 반드시 `Transform.scale`을 쓸 것.** 리그는 (이름, 픽셀)로
  디코딩해 둔 조각만 그린다 — 위젯 높이를 매 프레임 바꾸면 요청 픽셀이 매번
  달라져 캐시에 하나도 안 맞고, 그러면 **카피가 아예 안 그려진다.** 실제로
  뾱 커지는 1초 동안 화면이 텅 비었다. 축은 `bottomCenter`(발이 땅에 붙어야
  커지는 것이지 떠오르는 것이 아니다).
- **조각을 먼저 받고 시작한다**(`didChangeDependencies`에서 `CapySkins.load`).
  시간축만 흘러가면 로딩이 끝나는 순간 이미 걸어 들어온 짝꿍이 툭 나타난다 —
  스플래시에서 겪은 것과 같은 실수다.
- **첫째가 다 크는 판(190+40×3=310)부터는 독립과 출산이 같은 판에 겹친다**(첫째가 다 크는 판 = 새 아기가
  태어나는 판). 배치도 그대로라(첫째가 나가고 막내가 들어온다) 연출이 없으면
  정말 아무 일도 없었던 것으로 보인다. **떠나는 쪽을 먼저** 보여줘 "떠난 자리에
  새 아기가 온다"는 한 이야기로 잇는다. 반대로 두면 그냥 소란스럽다.
- `family.seen`은 **처음 물어보면 지금 레벨로 초기화한다.** 업데이트로 이 기능이
  들어왔다고 레벨 300짜리 사용자에게 결혼식부터 다시 보여줄 수는 없다.
  디버그로 크게 건너뛰면 최근 3개만 재생한다(수십 개면 몇 분을 갇힌다).
- **건너뛰기 버튼이 없다.** 6초짜리에 버튼을 달면 아무도 안 보고, 그러면 이
  게임 후반에는 볼 것이 하나도 없다. 대신 탭하면 **결말로 건너뛴다**(나가는
  것이 아니라 마지막 자세와 자막을 바로 보여준다). 안내 문구도 끝난 뒤에 뜬다.
- **독립만 결이 다르다.** 여기서는 아무도 폴짝 뛰지 않고 하트도 색종이도 없다.
  꾸벅 인사 → 걸어가다 **한 번 멈춰 뒤돌아보고** → 작아지고 옅어지며 사라진다.
  배경에 노을이 깔린다. 같은 몸짓을 쓰면 이별이 축하와 같은 사건이 된다.
- **소리가 사건을 가른다**(`tool/gen_voice.py`). 셋을 같은 소리로 때우면 결혼도
  출산도 이별도 같은 일로 들린다. 피치와 길이로 갈라 뒀다: `voice_love`(둘이
  겹쳐 부르는 화음) / `voice_baby`(2.15배로 올린 아주 짧은 소리) / `voice_bye`
  (1.34배로만 올려 길게, 여운) + `sparkle`(올라가는 세 음 — 목소리는 카피가
  내는 소리고 이건 **화면이 내는 소리**라 겹쳐야 장면이 커진다).
  - **`say`는 실행마다 결과가 미세하게 다르다**(길이가 2~3% 흔들린다).
    새 소리 하나 뽑겠다고 전부 다시 돌리면 검증해 둔 목소리까지 갈린다.
    그래서 `gen_voice.py`는 이름을 인자로 받는다: `python3 tool/gen_voice.py voice_love`.
- **알려진 한계 — 식구가 서로 많이 겹친다.** 캔버스 하나가 화면 폭의 2/3라
  `x`(-1~1)로 옮길 수 있는 거리가 좌우 50px 남짓뿐이다. 다섯이 서면 앞의
  아이가 부모를 절반 넘게 가린다. 제대로 고치려면 식구 수에 따라 `capyH`를
  줄여야 하고, 그건 홈 배치를 통째로 다시 잡는 일이다.

- **디버그 선택기**(`kDebugStages`, `_DebugStageJump`, `Progress.debugSetLevel`,
  `FamilyEvents.debugRewind`):
  성장·가족 변화는 400판 넘게 걸려 그냥은 확인할 수 없다. 홈 오른쪽 위에서
  레벨을 건너뛰고, 옆의 포크 버튼이 먹이를 채운다(먹는 연출을 보려고 매번
  한 판씩 깰 수는 없다). 레벨을 고르면 그 판의 사건 연출도 다시 재생된다.
  **정식 배포 전에 이것들을 함께 지울 것.**

## 화면 셋 — 게임처럼 보이게 (2026-08-20 오전)

카드와 버튼을 세로로 쌓아 둔 이전 홈은 "사무 앱 같다"는 판정을 받았다.
지금의 원칙은 **버튼처럼 생긴 것을 최소로 둔다**이다. 홈에서 버튼처럼 보여도
되는 건 시작 버튼 하나뿐이고, 먹이 주기는 "바구니를 눌러 당근을 던지는 것"이다.

- **홈**(`lib/main.dart`) = 카피가 사는 초원. 배경 `assets/scene/meadow.png`
  (Canva 생성) 위에 `MeadowGround`로 발밑 풀밭을 **직접 그린다** — 화면 비율에
  따라 배경 사진이 잘리는 위치가 달라져서 "발이 닿는 선"을 사진에 맡길 수 없다.
  오른쪽 당근 바구니를 누르면 당근이 포물선으로 날아가 **그 단계 캐릭터의
  입**에 물린다(`CapySkins.mouthOf`) — 입 높이는 캐릭터마다 다르다. 예전엔
  원본 카피 기준 값 하나를 모든 단계에 써서 아기 머리 위 허공에서 당근이
  씹혔다. 먹이 크기도 덩치에 맞춘다. 거기서부터
  `CapyAct.eat`으로 와구와구 씹히며 `CarrotPainter(eaten:)`로 조금씩 사라진다.
  **그냥 사라지게 하면 "먹었다"가 아니라 "삭제됐다"로 보인다.**
  홈의 세로 순서는 사용자가 그려 준 배치 그대로다:
  포만감·기분 게이지(위쪽 양 끝으로 벌림) → 말풍선 → **이름표** → 카피 →
  **몸무게** → 성장 게이지 → 레벨 진행 → 오늘의 퍼즐.
  이름은 위, 몸무게는 아래다. 먹이 둘(당근 바구니·수박)은 오른쪽에 **같은
  크기로 세로**로 선다 — 그림마다 후광·여백이 달라서 같은 숫자로 그리면
  크기가 달라 보인다. 눈에 같아 보이도록 배율을 따로 맞춰 뒀다.
  **없는 먹이를 눌렀을 때는 누른 자리만 흔들려야 한다** — 수박을 눌렀는데
  당근 바구니가 흔들리면 무엇이 없다는 건지 알 수 없다(실제로 겪은 버그).
- **이름**(`Pet.name`, `pet.name` 키) — 지어 주기 전에는 말풍선이 조른다.
  이름이 붙는 순간 애착이 생긴다는 것이 이 기능의 전부다.
- **오늘의 퍼즐**(`Levels.dailyPuzzleOf`) — 날짜(yyyymmdd)가 곧 시드라 서버 없이
  전 세계가 같은 날 같은 판을 푼다. **늘 8×8에 가장 어려운 후보**(한 수 읽기가
  있는 판 우선)이고 스타터 카피도 안 놓아 준다 — 쉬우면 존재 이유가 없다.
  시드 대역은 9억대(레벨 시드 `level*100+k`와 절대 안 겹친다).
  보상은 당근 5개(보통 판은 1~2개), 연속 기록(`daily.streak`)이 재방문을 만든다.
  저장 슬롯은 `bd.d20260820` — `Progress.saveBoard`가 문자열 슬롯을 받는 이유다
  (레벨은 `'$level'`을 그대로 넘겨 예전 저장분이 살아 있다).
- **스플래시**(`lib/ui/splash.dart`) — 카피가 뿅 튀어나오고 이름이 한 글자씩
  떨어지고 로딩 바가 찬다. 준비가 먼저 끝나도 연출은 끝까지 간다(뚝 끊기면
  버그처럼 보인다).
  **연출보다 `CapyRigImages.load()`가 먼저다.** 조각이 없으면 카피가 아예 안
  그려지는데 등장 곡선은 계속 흘러가서, 로딩이 끝나는 순간 이미 커진 카피가
  툭 나타났다 되튀며 줄어드는 것처럼 보인다. 순서를 되돌리지 말 것.
  튀는 폭(`PoingCurve.overshoot`)도 화면 크기에 따라 다르다 — 칸만 한
  토큰은 0.28, 화면 한가운데의 250px짜리는 0.13이어야 덜컹거리지 않는다.
- **완성 화면**(`lib/game/win_celebration.dart`) — **불투명하다.** 뒤에 보드가
  비치면 판이 안 끝난 것처럼 읽힌다. 축하는 춤/폴짝/온천 셋 중 하나가
  레벨로 결정되어 나온다(같은 판은 늘 같은 축하). 배경은 따뜻한 노을 그라데이션.
- **아이콘도 판 위의 물건과 같아야 한다**(2026-08-20 저녁):
  - 상단 "몇 마리 놓았나"와 하단 힌트 버튼의 카피는 **칸에 들어가는 그 얼굴**
    (`CapyFaceIcon`)이다. **리그 조각을 그대로 겹쳐 쓰면 안 된다** — 머리
    조각의 아래쪽은 몸통과 이으려고 페더링돼 있어서 목이 안개처럼 흐려지며
    끊기고, 작게 그리면 그 자락이 얼룩으로 보인다. `rig_stages.py`가 알파를
    또렷하게 잘라(임계 110 + 0.9 블러) `h_face.png` 한 장으로 뽑는다.
    **퍼즐 칸의 얼굴(`h_head`)도 같은 처리를 한다** — 칸 안에서도 목이
    흐릿하게 끊겨 보였다. 단 **턱·눈꺼풀에는 하지 않는다**: 둘 다 머리에
    부드럽게 얹혀야 하는 조각이라 또렷하게 자르면 타원 테두리가 드러난다.
    자르는 상자는 넷이 **같아야 한다**(또렷하게 자르기 전 머리 기준) —
    조각마다 제 알파로 자르면 서로 어긋나 턱이 옆으로 밀린다.
    크림 배경에서 흐려 보이지 않게 채도·대비도 아주 살짝 올린다(1.14/1.07) —
    **아이콘에서만**. 판 위의 카피와 색이 달라지면 안 된다.
    벡터 카피(`capyToken`)는 판 위의 카피와 다르게 생겨서 같은 게임의
    물건으로 안 읽혔다. 살아 움직일 자리가 아니라 `CapyPerformer`는 안 쓴다.
  - 힌트 버튼은 **흰 동그라미** 위에 얹는다(레퍼런스도 같다). 크림 배경에
    그림만 놓으면 떠 보이고 눌리는 것으로 안 읽힌다. 한때 원을 뺐다가
    되돌렸다 — 뺄 이유였던 "스티커처럼 보인다"는 벡터 카피 때 이야기였고,
    지금은 판 위와 같은 얼굴이라 원 안에 있어도 그 캐릭터로 읽힌다.
    **얼굴과 발바닥은 눈에 같은 크기로** 맞춘다(발바닥은 여백이 적어 같은
    숫자로 그리면 훨씬 작아 보인다).
  - X 힌트는 전구가 아니라 **발바닥**(`pawIcon`)이다. 둘 다 "어디인지"를
    짚는 힌트라 얼굴과 발바닥 한 쌍이 맞다(레퍼런스도 고양이 얼굴+발바닥).
  - 목숨은 **하트**다. 귤 → 당근 → 하트로 두 번 옮겼다. 귤은 게임 안
    어디에도 없는 물건이라 무엇을 잃은 것인지 안 읽혔고, 당근으로 바꿨더니
    바로 옆에 **모으는 당근** 카운터가 생겨 같은 그림이 "늘어나는 것"과
    "줄어드는 것" 둘 다가 되어 버렸다. 하트는 설명 없이 통하는 유일한
    기호이고, 당근(모은다)과 의미 축이 완전히 갈린다. **되돌리지 말 것.**
    잃은 하트는 같은 실루엣을 옅게 칠한다 — 지우면 원래 몇 개였는지 모른다.
  - 개수 배지는 원이 아니라 **라운드 8**이다.
- **타일 토큰**(`_CapyToken`) — 얼굴 스킨 + 흰 테두리, 판 전체가 맞춰 움직인다.
  놓이는 순간 `PoingIn`(한 번만 크게 넘겼다 되튀는 곡선 — `elasticOut`은 여러 번
  흔들려 물컹해 보인다) + `PoingBurst`(충격파 링·별·먼지)가 **카피 앞에서** 터진다.
  뒤에 두면 얼굴에 가려 안 보인다. 크기는 가로 기준으로 맞춘다(얼굴이 세로보다
  넓어서 높이로 맞추면 귀가 잘린다) + 크게 도리질하므로 여유 13%를 남긴다.

## 카피 키우기 (다마고치 메타) + 사운드 (2026-08-20 심야 2차)

- **Pet** (`lib/pet/pet.dart`): 포만감·기분·몸무게·성장. **밸런스는 파일 상단
  주석이 원본** — 몸무게는 기준체중×(1±25%) 수렴형이라 절대 발산하지 않는다
  (시간당 +1kg식 선형 설계 금지). 오프라인 소급 정산(시간 단위 순차).
- 보상 루프: 클리어 → 당근(판 크기 비례 1~2) + 7판마다 황금귤 → 홈에서 먹이기
  → 포만감/기분/체형 변화. 클리어 자체가 기분 +8 (게임이 곧 돌봄).
- 홈 = 관리 화면: 단계 칩, 상태 말풍선, 터치 쓰다듬기(3분 쿨), 게이지 2종,
  몸무게·체형, 먹이 버튼, "다음 성장까지 N판" 훅.
- 성장 단계는 게임 레벨: 아기(1)/어린이(17)/청년(34)/어른(50). 단계·체형별
  전용 렌더는 프롬프트 팩으로 생성 대기 — 그 전까지 크기/가로 스케일로 표현.
- **사운드**: 합성음(`tool/gen_sfx.py`)만으로는 "삑" 소리밖에 안 난다.
  **카피 목소리는 `tool/gen_voice.py`가 macOS 한국어 TTS를 재료로 만든다** —
  사람이 말한 "카피"를 1.6배 빠르게 재생하면 작은 동물이 재잘대는 소리가 된다.
  성우 없이 얻을 수 있는 가장 그럴듯한 방법이다.
  voice_place(카피~카피~)·voice_wrong(꽥)·voice_win / tick(X 한 칸, 아주 짧아서
  드래그하면 "띠디디디딕") / munch·pet.
  - **44.1kHz다.** 22.05kHz였을 때는 종소리·반짝임처럼 고음이 중요한 소리가
    통째로 먹먹했다. 합성음은 잔향 없이 내보내면 싸구려 삐삐로 들리므로
    30~140ms 다중 탭 잔향을 섞는다(`reverb`). 음정은 5음 음계에서만 골라
    아무 둘이 겹쳐도 불협이 안 된다.
  - **오디오 포커스를 잡지 않는다**(`Sfx.init`). 기본값은 소리마다 시스템
    포커스를 뺏고 돌려주는데, X를 쭉 그으면 40ms 간격이라 앞소리가 잘리고
    사용자가 듣던 음악도 계속 끊긴다.
  - 판을 깨면 **뾰로롱** 아르페지오가 먼저 나가고 320ms 뒤에 목소리가 얹힌다.
    목소리 하나뿐일 때는 판을 깬 순간이 칸 하나 맞힌 순간과 비슷하게 들렸다.
  - "소리가 안 난다"고 했을 때 실은 **나고 있었다**. 파일 피크가 35~57%인데
    볼륨까지 0.4~0.6을 걸어 실효 20~35%였던 것. 이제 정규화(82%)하고 볼륨을
    올렸다. `Sfx`는 예외를 삼키지 않고 디버그에 남긴다 — 삼키면 영영 못 찾는다.
- **설정**(`lib/core/settings.dart`, `lib/ui/settings_sheet.dart`): 소리·진동
  끄기. **진동은 전부 `Buzz`를 지나간다** — 호출부마다 조건을 적으면 한 군데는
  반드시 빠뜨린다.
- 미구현 메모: 배고픔 로컬 푸시("주인님 배고파요") — flutter_local_notifications,
  포만감<25에서 하루 1회. 알림 권한·정책 검토 필요해 보류.

## 판을 되감을 수 없다 (2026-08-21)

**같은 레벨은 영원히 같은 판이다.** 그래서 판을 되돌릴 수단이 있으면 그게
곧 정답 캐기가 된다 — 아무 데나 찍어 보고, 되감고, 외운 자리에 놓으면 된다.
하트는 아무 제약이 아니게 되고 리워드 광고를 볼 이유도 사라진다.

- 게임 화면의 **새로고침 버튼을 없앴다**(사용자 지적).
- **하트를 판과 함께 저장한다.** 안 하면 홈에 갔다 오는 것만으로 완충이었다.
  `bdm.*`의 가운데 두 자리(원래 "주운 당근", 지금은 안 쓴다)를 물려받았다.
- 다시 푸는 유일한 길은 **하트를 다 잃는 것**이고, 그 자리가 곧 리워드
  광고를 권하는 자리다.

## 반들거림과 역동성 (2026-08-21)

- **타일은 단색이 아니라 그라디언트다**(`Palette.glossy`). 위를 밝히고 아래를
  살짝 어둡게 하는 것만으로 평면이 입체가 된다 — 그림이 전체적으로 납작해
  보이던 가장 큰 이유가 이거였다. 흰색을 덧씌우면 색이 바래므로 HSL에서
  밝기만 움직인다.
- **칸에 놓이는 카피는 앞에서 크게 날아와 꽂힌다**(`PoingDrop`). 0에서
  부풀어 오르던 예전 등장(`PoingIn`)은 "생겼다"로 읽혔지 박히는 맛이 없었다.
  꽂힌 뒤 `GlossSweep`이 표면을 한 번 훑는다.
- **틀리면 빨간 X + 판 전체가 부르르.** 놀란 카피 얼굴을 띄우던 것을 바꿨다 —
  틀린 칸에 카피가 있으면 "놓였다"와 "놓으면 안 된다"가 같은 그림이 된다.
  칸 하나만 빨개지면 눈이 딴 데 가 있을 때 놓치므로 판을 흔든다.
- **`CapyAct.wiggle`(엎드려 엉덩이 털기)과 `twirl`이 있다.** 폴짝과 좌우
  흔들기는 둘 다 몸을 세운 채라 서로 비슷해 보였는데, wiggle은 실루엣 자체가
  바뀐다. 완료 화면은 흔들기 → 엉덩이 털기 → 폴짝을 **돌아가며** 낸다.
  한 동작만 되풀이하면 세 번째부터는 배경이 된다.
- 홈의 무심한 동작은 **셋에 둘이 큰 동작**이다. 절반이었을 때도 "정지 화면
  같다"는 말을 들었다 — 고개 까딱은 멀리서 보면 안 움직이는 것과 같다.

## 다음

- **스프라이트 프레임 계획은 폐기했다.** 얼굴 6종·전신 4프레임을 생성해
  갈아 끼우려던 방향은 리그로 대체됐다(각도가 연속값이라 장수가 필요 없다).
  새 동작이 필요하면 이미지를 만들지 말고 `CapyAct`에 케이스를 하나 추가한다.
- 리그로 못 하는 자세(누워서 배 긁기, 옆모습)는 **새 원본 렌더**가 필요하고,
  그때는 `rig_parts.py`의 좌표 상수를 그 그림에 맞춰 다시 재야 한다.
- 릴리스 APK가 66MB, AAB가 67MB다(에셋과 광고 SDK가 대부분). Play 상한은
  넉넉하지만 줄일 여지가 있는지 한 번 볼 것.

## 아직 없는 것 (nemologic에서 이식)

- 오늘의 퍼즐 + 스트릭, 주간 트레이닝 차트
- 튜토리얼(규칙 칩이 절반은 해주지만 첫 판 인터랙티브 필요)
- 광고(`lib/core/ads.dart`): 배너는 레벨 4부터 게임 화면 하단, 전면은
  **레벨 20부터 다섯 판마다**(판 사이에만), 리워드는 힌트·하트 충전.
  - 깬 판 수는 **저장한다.** 예전엔 메모리 static으로 세서 앱을 껐다 켜면
    0으로 돌아갔고, 한 번에 서너 판 하는 보통 사용자는 전면 광고를 평생
    한 번도 못 봤다 — "일곱 판마다"는 코드에만 있던 말이었다.
  - 성장·결혼 장면이 대기 중이면 건너뛴다. 이 게임의 유일한 사건 앞을
    광고로 막지 않는다.
  - 띄울지 판단하는 규칙은 `Ads.shouldShowAfterClear`로 떼어 두었고
    `test/ads_pacing_test.dart`가 검사한다.
- proguard(광고 붙일 때 nemologic에서 복사), 실기기 확인

## 밸런스는 재서 정한다

`flutter test test/audit_manual.dart`가 난이도 곡선·성장 도달 시점·당근
수지·광고량을 한 번에 출력한다. **통과/실패가 아니라 읽으라고 만든 것**이라
이름이 `_test.dart`로 안 끝난다(기본 실행에 안 걸린다).

여기서 잡은 것들:
- 성장 간격 50 → 30판(어른까지 33일 → 20일).
- 당근 하나의 포만감 회복 22 → 12. 22일 때는 하루 15개 벌어 3개만 쓰여
  **30일에 397개가 남았다.** 남아도는 보상은 보상이 아니다.
- 기분 감소 시간당 2 → 1. 채울 길이 당근(+4)·수박(+18)·쓰다듬기(+6)뿐이라
  며칠이면 바닥에 눌어붙었고, 늘 시무룩한 카피는 돌볼 맛이 안 난다.

상수를 고치면 **다시 돌려 볼 것.** 감으로 "좀 많은 것 같다"고 말하지 않기
위한 도구다.

## 두 언어 (2026-08-21)

영어 등록정보만 올리면 영어권 사용자가 받아서 **한국어 앱**을 만난다 —
별점 1점을 부르는 조합이라 앱을 먼저 영문화했다. 코드에 박혀 있던 한글
문자열이 293개였고, 그중 137개가 `capy_says.dart`(카피의 목소리)였다.

- **패키지도 코드 생성도 쓰지 않는다**(`lib/core/lang.dart`).
  `flutter_localizations` + ARB(`gen-l10n`)가 표준이지만, 문구 300개짜리
  오프라인 단일 개발자 앱에는 과하다 — 빌드 단계가 하나 늘고 문구를 고칠
  때마다 코드 생성을 돌려야 한다("유지보수 없는 앱" 전제와 어긋난다).
- **`L.t(한국어, English)`가 한 줄에 두 언어를 나란히 들고 있다.** 문구를
  고칠 때 짝을 눈으로 같이 보게 되므로 **한쪽만 고쳐 두고 잊는 사고가 안
  난다.** 문구 파일이 따로 없어 "이 문장이 어디 나오는지"를 찾을 필요도 없다.
- 목록은 `L.pickList(ko, en)`. 카피의 목소리처럼 **줄 수가 다를 수 있는**
  것은 번역이 아니라 다시 쓰는 것이라 언어마다 목록이 따로다.
  **새 문구는 두 목록에 함께** 넣을 것 — 한쪽만 늘리면 인덱스로 고르는
  자리에서 언어에 따라 다른 말이 나온다.
- **`const` 표에서는 `L.t`를 부를 수 없다.** `Pet.stages`처럼 상수로 두어야
  하는 표는 이름을 **두 벌 들고**(`name`/`nameEn`) `label` 게터로 고른다.
  위젯도 마찬가지 — `const Text(L.t(...))`는 컴파일이 안 되므로 `const`를
  바깥에서 안쪽으로 옮긴다.
- 언어는 켤 때 한 번 정한다. **기기 언어가 한국어면 한국어, 나머지는 전부
  영어**(못 읽는 한글보다는 읽히는 영어다). 설정에서 기기 설정/한국어/English를
  고를 수 있다 — 한국에 사는 영어 사용자, 해외에 사는 한국어 사용자가 둘 다
  있고, **무엇보다 이게 없으면 영어 화면을 확인할 길이 기기 언어를 바꾸는
  것뿐이다.**
- **카피의 목소리는 번역이 아니라 다시 썼다.** 게으르고 센치하고 살짝 웃긴
  말투가 이 캐릭터 자체인데, 직역하면 셋 다 날아가고 평범한 퍼즐 게임 문구가
  된다. 줄 수와 줄바꿈 자리는 맞춰 두었다 — 말풍선 크기가 문구마다 흔들리면
  화면이 덜컹거린다.
- 폰트 Jua는 라틴 문자가 들어 있어 그대로 쓴다. `'Apple SD Gothic Neo'`를
  지정한 자리는 영어 기기에서 시스템 폰트로 떨어진다(문제 없음).
- 디버그 전용 문구(`kDebugStages`의 레벨 점프 라벨)는 한국어로 두었다 —
  릴리스에서 통째로 빠진다.

## 출시

준비물은 `store/`에 전부 있다. 남은 것은 **사람만 할 수 있는 두 가지**뿐이다.

1. **AdMob 실제 ID** — `lib/core/ads.dart`와 `AndroidManifest.xml` **두 곳**.
   지금은 구글 테스트 ID다.
2. ~~업로드 키스토어~~ → **끝났다.** 키는 저장소 **밖**
   `../keystore/`에 두 앱 것을 함께 둔다 — 한때 photo_tidy는 키를 저장소
   **안에** 두고 커밋까지 했었다(비공개라 살았다). `.gitignore`는 한 번만
   잘못 건드리면 뚫리므로 애초에 밖에 두는 편이 맞다. capydoku는 방침을
   GitHub Pages로 띄우느라 **공개 저장소**다.
   - **경로에 한글이 있으면 `Properties.load(InputStream)`가 깨뜨린다**
     (기본 인코딩 ISO-8859-1). `build.gradle.kts`에서 `f.reader(Charsets.UTF_8)`로
     읽는다 — 되돌리면 "키스토어 파일 없음"으로 빌드가 죽는다.

절차와 콘솔 입력값은 `store/RELEASE.md`. 문구는 `store/listing.md`,
데이터 안전 답안은 `store/data_safety.md`, 방침은 `store/privacy.md`.
스크린샷(1080×1920)은 `store/screenshots/`, 아이콘·피처 그래픽은
`store/graphics/`(`tool/gen_store_graphics.py`가 만든다).

디버그 도구(레벨 점프·먹이 채우기)는 **지우지 않고** `kDebugStages =
kDebugMode`로 묶어 두었다. 릴리스에서는 트리 셰이킹으로 통째로 빠진다.
손으로 껐다 켜면 켠 채로 올리는 사고가 언젠가 난다.
