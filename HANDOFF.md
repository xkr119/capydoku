# 세션 인수인계 (2026-08-21)

다음 세션이 이 파일만 읽고 이어서 작업할 수 있도록 남긴다.
**설계 판단의 "왜"는 `CLAUDE.md`에 있다** — 여기는 지금 상태와 다음에 할 일이다.

---

## 지금 어디까지 왔나

Capydoku는 **기능적으로 완성**됐고 출시 준비물도 다 만들어져 있다.
남은 건 사람만 할 수 있는 두 가지(AdMob 실제 ID, 업로드 키스토어)와
폐쇄 테스트 12명×14일이다. 전부 `store/RELEASE.md`에 적혀 있다.

- 저장소: `/Users/tak/개발/capydoku` (git, `main` 브랜치, 미커밋 없음)
- 최신 커밋: `480aa9c 아이콘: 아기 얼굴만, 보라+주황...`
- 테스트: 33개 전부 통과 (`flutter test`)
- 폰(Galaxy S23 Ultra `R3CX4001P2B`)에 **디버그 빌드** 설치돼 있음

### 이번 세션에 한 일 (커밋 순서대로)

1. **출시 준비 일체** — 아이콘, 서명 설정(`android/key.properties` gitignore),
   스토어 문구·개인정보처리방침·데이터 안전 답안, 스크린샷 5장, AAB 빌드
2. **완료 화면 개편** — 즉시 춤, 큰 숫자, 같은 크기 버튼, 온천 변형 제거
3. **튜토리얼** — 첫 실행에 규칙 4장 → 레벨 1 → 초원 안내 4곳 → 레벨 2
4. **사운드 전면 재작업** — 44.1kHz, 잔향, 뾰로롱 클리어음, 빈 자리 6곳 채움,
   오디오 포커스 안 잡기
5. **레벨 커브** — 레벨 10부터 10×10, 시드를 미리 구움(레벨 12 2.3초 → 0.16초)
6. **광고 페이싱** — 레벨 20부터 5판마다, 최소 4분, 사건 앞에서는 건너뛰기.
   깬 판 수를 저장(예전엔 메모리라 사실상 전면 광고를 아무도 못 봤다)
7. **되감기 차단** — 새로고침 버튼 제거, 하트를 판과 함께 저장
8. **출석 화면** — 창이 아니라 초원 장면. 짝꿍이 선물을 가슴에 안고 기다린다
9. **역동성** — 빨간 X + 판 흔들림, 유리 타일, 꽂히는 등장, 엉덩이 털기(wiggle)
10. **밸런스 점검** — 성장 30판 간격, 먹이 효율·기분 조정 (아래 참고)
11. **아이콘** — 여기서 제일 많이 헤맸다 (아래 참고)

---

## 밸런스: 이번에 잰 값과 바꾼 것

`flutter test test/audit_manual.dart`가 난이도 곡선·성장 도달 시점·당근 수지·
광고량을 한 번에 출력한다. **통과/실패가 아니라 읽으라고 만든 것**이라 이름이
`_test.dart`로 끝나지 않는다. 상수를 고치면 다시 돌려 볼 것.

| 항목 | 값 | 판단 |
|---|---|---|
| 난이도 | 40판 구간 평균 4.4 → 28.0, 한수읽기 0 → 5.4 | 좋음 |
| 성장 | **30판마다** (어른 16일·결혼 22일·첫째 31일, 하루 20분 기준) | 이번에 당김 |
| 당근 | 판당 2개, 30일에 337개 남음 | **사용자가 "괜찮다"고 판단** |
| 기분 | 시간당 -2, 당근 +8 | 이번에 조정 |
| 포만감 | 15분에 -1(시간당 4), 당근 +12 | 이번에 조정 |
| 전면 광고 | 하루 1.1회 | 적정 |
| 힌트 | 판당 1개꼴 | 적정 |

**당근이 쌓이는 건 구조적이다.** 소비는 시간이 정하고(포만감 상한 100)
수입은 판 수가 정하니, 효율을 낮춰도 많이 푸는 사람에겐 계속 남는다.
사용자는 "먹이 주는 재미니까 쌓여도 된다"고 결론 냈다 — **다시 건드리지 말 것.**
정말 잡아야 할 때가 오면 판 수에 비례해 쓰는 곳(힌트를 당근으로, 성장에 당근
요구)을 하나 더 만드는 수밖에 없다.

---

## 아이콘: 세 번 헛돈 이유

**`assets/icon/icon.png`만 보고 판단하면 안 된다.** 요즘 폰은 적응형 아이콘을
쓰고 그건 **바깥 1/3이 잘려 나간다**(108dp 중 가운데 72dp만 보인다).
레거시 파일만 열어 보고 "괜찮네" 하다가 턱이 잘리고 배경이 한 칸만 남은
아이콘을 세 번 내보냈다.

**반드시 `python3 tool/preview_icon.py`로 확인한다.** 런처가 실제로 자르는
대로(겹치고 → 가운데 66%만 → 마스크 모양대로) 그려 준다.

지금 아이콘의 규칙:
- 얼굴은 **아기 단계**(`ICON_FACE = 'stage1'`)에서 뽑고 아래 1/4(목·가슴
  페더링)을 잘라 **얼굴만** 쓴다.
- 얼굴에 **흰 테두리**를 한 바퀴 두른다. 카피 털은 가장자리가 부슬부슬해서
  이게 없으면 배경에 녹아 런처에서 혼자 뿌옇다.
- 전경은 보이는 영역 안에 **통째로** 들어와야 한다(0.615). 더 키우면 테두리가
  잘리고, 테두리가 잘리면 있으나 마나다.
- 배경은 **진보라 + 주황** 두 색, 흰 선은 아주 얇게(0.008). 얇은 선은 회전
  보간에 흐려지므로 **세 배로 그린 뒤 줄인다**(수퍼샘플링).

---

## 다음에 할 일

### 출시를 막는 것
1. **AdMob 실제 ID** — `lib/core/ads.dart`와 `AndroidManifest.xml` **두 곳**.
   지금은 구글 테스트 ID다.
2. **업로드 키스토어** — `android/key.properties.example`대로 만든다. 없으면
   릴리스가 디버그 키로 서명되고 Play가 거부한다.
3. **폐쇄 테스트 12명 × 14일** — 개인 계정이면 앱이 다 돼 있어도 3주가 더 걸린다.

### 남은 다듬을 것
- **스토어 스크린샷 1번**을 판이 아니라 **아기/어린이 카피가 크게 나온 것**으로
  바꾸는 게 좋다. 목록에서 첫 장이 썸네일인데 지금은 "또 스도쿠"로 읽힌다.
- **어른(stage5) 눈**이 아직 작다. 캔바 할당량이 돌아오면 다시 렌더.
- **짝꿍(mate) 팔을 안 나눈다** — 앞발이 배에 붙은 렌더라 도려낼 수가 없다.
  먹을 때 앞발을 못 든다. 새 렌더가 있어야 풀린다.
- **우는 카피가 귤을 들고 있다**(`capy_cry.png`). 이 게임에 귤은 없다.
  사용자가 "괜찮다"고 했지만 렌더가 가능해지면 고칠 것.
- **홈 16:9 화면**에서 이름표가 카피 다리와 살짝 겹친다.

### 손대면 안 되는 것
- **레벨 → 퍼즐 매핑**(`lib/game/levels.dart`, `level_seeds.dart`).
  출시 후 바꾸면 전 사용자의 판이 통째로 바뀐다. 시드를 다시 굽지 말 것.
- **성장·가족 표**(`pet.dart`, `family.dart`). 바꾸면 카피가 갑자기 늙거나
  젊어진다.
- **당근 경제** — 위에 적은 대로 사용자 판단이 끝났다.

---

## 작업 환경

```bash
export PATH="$HOME/development/flutter/bin:$HOME/Library/Android/sdk/platform-tools:$PATH"
cd /Users/tak/개발/capydoku

flutter test                              # 33개
flutter test test/audit_manual.dart       # 밸런스 수치 출력
python3 tool/preview_icon.py              # 런처에 보이는 대로 아이콘 확인

flutter build apk --debug                 # 디버그(⏩ 건너뛰기·레벨 점프 포함)
flutter build apk --release               # 배포용
flutter build appbundle --release         # 스토어용

adb -s R3CX4001P2B install -r build/app/outputs/flutter-apk/app-debug.apk
```

**디버그 빌드에만 있는 것**(`kDebugStages = kDebugMode`, 릴리스에서 트리 셰이킹):
- 퍼즐 화면 우상단 **⏩** — 판을 통째로 채우고 바로 완료
- 홈 상단 **🎓** 튜토리얼 다시 보기, **🍴** 먹이 채우기, **DEBUG** 레벨 점프

**에뮬레이터 상태 조작**(빠른 확인용):
```bash
adb root
P=/data/data/kr.tak.capydoku/shared_prefs/FlutterSharedPreferences.xml
adb shell "sed -i 's|name=\"flutter.level.current\" value=\"[0-9]*\"|name=\"flutter.level.current\" value=\"120\"|' $P"
```

**에셋 파이프라인** (원본은 `build/stage_raw/`, gitignore 아님에 주의):
```bash
python3 tool/bigger_eyes.py    # 청소년·성인·어른 눈 부풀리기
python3 tool/gen_stages.py     # 원본 → assets/stages
python3 tool/rig_stages.py     # assets/stages → assets/rig (좌표도 출력)
python3 tool/gen_sfx.py        # 효과음 합성
python3 tool/gen_icon.py       # 아이콘
dart run flutter_launcher_icons
python3 tool/gen_store_graphics.py
```

**캔바 할당량이 소진돼 있다**(2026-08-21 기준). 새 캐릭터 렌더가 필요한
작업은 할당량이 돌아온 뒤에.
